#include <bits/stdc++.h>
using namespace std ;
void sortedArray(int *arr , int n){
    // base case 
    if(n==0 || n==1){
        return;
    }

    // recursion relation 
    for(int i=0 ; i<n-1 ; i++){
        if(arr[i] > arr[i+1]){
            
        }
    }
}
int main()
{
        int n;
    cout << " enter the sizee of the array " << endl;
    cin >> n;

    int arr[100];
    for(int i=0 ; i<n ; i++){
        cin >> arr[i];
    }



    
    return 0;
}