#include <bits/stdc++.h>
using namespace std ;
int squarroot(int n){
   int ans = pow(n,0.5);
   return ans;
}
int main()
{
    int n;
    cin >> n;

    cout << squarroot(n);
    

    
    return 0;
}